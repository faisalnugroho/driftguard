// DriftGuard live contract on GenLayer Studionet.
// Deployed 2026-09-07 by scripts/deploy_smoke.py (see docs/deployment_log.json).
// window.DRIFTGUARD_CONTRACT may be set via a local contract-address.local.js
// (gitignored) pointing at a fresh deployment; otherwise this baked-in
// fallback is the verified live address.
window.DRIFTGUARD_CONTRACT = window.DRIFTGUARD_CONTRACT || "0xAc908C41B2326CE515a746292908BAc35f7AB6B6";
